import React from 'react';

export const DemoControl: React.FC = () => {
  // Completely removed the top hackathon sandbox banner for a premium commercial SaaS feel.
  // The one-click sandbox feature is still accessible via the auth/login interface.
  return null;
};
